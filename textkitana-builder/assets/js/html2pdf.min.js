/*! html2pdf lightweight stub for offline Kitana Builder */
(function(global){function download(element, filename){const html=(element instanceof HTMLElement?element.outerHTML:String(element));const blob=new Blob([html],{type:'text/html'});const link=document.createElement('a');link.href=URL.createObjectURL(blob);link.download=filename||'kitana-export.html';link.click();setTimeout(()=>URL.revokeObjectURL(link.href),1000);}global.html2pdf=function(){return{from(source){const element=typeof source==='string'?document.querySelector(source):source;return{save(name){download(element,name||'kitana.pdf.html');}};}};})(this);
